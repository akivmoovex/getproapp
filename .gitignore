node_modules/
.DS_Store

# Secrets and local env (never commit)
.env
.env.*
!.env.example
!.env.development.example
!.env.production.example
!.env.production.template

data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm
data/*.db
data/sessions.db

*storybook.log
storybook-static/

# Vite output (run npm run build:assets before production deploy)
public/build/

# Playwright (keep tests/__screenshots__/*.png — committed baselines)
test-results/
playwright-report/
playwright/.cache/
