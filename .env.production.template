# =============================================================================
# This file is a TEMPLATE. Do NOT commit real .env.production to GitHub.
# =============================================================================
#
# USAGE (Hostinger / LiteSpeed Node)
# - Copy this file to `.env.production` on the server (fill with real values).
# - Deterministic fallbacks (see `buildProductionFallbackCandidates` in `src/startup/bootstrap.js`):
#   - pronline (path hints): /home/u549637099/pronline/.env.production
#   - getproapp (path hints): /home/u549637099/getpro/.env.production
#   Legacy suffixed files under the home directory and generic `.env.production` are still tried.
#   Optional GETPRO_PRODUCTION_ENV_FILE_FALLBACK is always tried first when set.
# - When NODE_ENV=production, the app does NOT load repo-root `.env`; it uses Hostinger
#   injected env first, then merges ONLY MISSING keys from this file (`override: false`).
# - Do NOT commit the filled `.env.production` file.
#
# =============================================================================

# --- PostgreSQL (set DATABASE_URL *or* GETPRO_DATABASE_URL; DATABASE_URL wins if both set) ---
DATABASE_URL=postgresql://REPLACE_ME_USER:REPLACE_ME_PASSWORD@REPLACE_ME_HOST:5432/REPLACE_ME_DB?sslmode=require

# Optional alternative name (same semantics as DATABASE_URL in app code):
# GETPRO_DATABASE_URL=

# --- Required for production session and URLs (see productionEnvGate) ---
SESSION_SECRET=REPLACE_ME_LONG_RANDOM_STRING
BASE_DOMAIN=REPLACE_ME.example.com

# --- Admin bootstrap / ops (never log values) ---
ADMIN_PASSWORD=REPLACE_ME

# --- Public URL scheme (https in production) ---
PUBLIC_SCHEME=https

# --- PostgreSQL SSL mode for node-pg (e.g. no-verify | strict | off) ---
GETPRO_PG_SSL=no-verify

# --- Optional diagnostics only (presence checks in logs; remove after debugging) ---
# DBURL_TEST=yes
