<style>
  /* Critical CSS for homepage LCP (hero + top bar layout). Keep tiny. */
  :root {
    color-scheme: light;
  }
  html,
  body {
    margin: 0;
    padding: 0;
  }
  body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    background: #f6f7fb;
    color: #121417;
  }
  /* Match loaded `styles.css` public band: 1200px + gutters (24px mobile, 32px ≥768px). */
  .container,
  .ds-container {
    box-sizing: border-box;
    width: 100%;
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
    padding-left: 24px;
    padding-right: 24px;
  }

  @media (min-width: 768px) {
    .container,
    .ds-container {
      padding-left: 32px;
      padding-right: 32px;
    }
  }
  .app-layout {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }
  .app-layout__main {
    flex: 1 0 auto;
    width: 100%;
  }
  .app-top-app-bar {
    background: #f6f7fb;
    border-bottom: 1px solid #e4e8f0;
  }
  .app-top-app-bar__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 12px 0;
    gap: 12px;
  }
  .gp-home-hero {
    position: relative;
  }
  .gp-home-hero__bg {
    position: relative;
    overflow: hidden;
    background: #0b0b10;
  }
  .gp-home-hero__photo {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    object-fit: cover;
  }
  .gp-home-hero__scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.55) 0%, rgba(0, 0, 0, 0.25) 45%, rgba(0, 0, 0, 0.35) 100%);
    pointer-events: none;
  }
  .gp-home-hero__content {
    position: relative;
    margin-top: -240px;
    padding-bottom: 28px;
  }
  .gp-home-hero__title {
    margin: 0;
    color: rgba(255, 255, 255, 0.96);
    text-shadow: 0 2px 18px rgba(0, 0, 0, 0.35);
    font-weight: 900;
    letter-spacing: -0.02em;
    font-size: 2.1rem;
    line-height: 1.1;
  }
  .gp-home-hero__subtext {
    margin: 8px 0 0;
    color: rgba(255, 255, 255, 0.88);
    text-shadow: 0 2px 18px rgba(0, 0, 0, 0.35);
    font-size: 1rem;
    line-height: 1.45;
    max-width: 60ch;
  }
  .gp-home-search-card {
    margin-top: 16px;
    background: rgba(255, 255, 255, 0.96);
    border: 1px solid rgba(228, 232, 240, 0.9);
    border-radius: 22px;
    box-shadow: 0 14px 44px rgba(15, 23, 42, 0.14);
    padding: 16px;
  }
  @media (max-width: 420px) {
    .gp-home-hero__title {
      font-size: 1.85rem;
    }
  }

  @media (max-width: 768px) {
    .gp-home-hero__content {
      margin-top: -200px;
      padding-bottom: 18px;
    }
    .gp-home-search-card {
      border-radius: 20px;
      padding: 16px;
    }
  }
</style>
