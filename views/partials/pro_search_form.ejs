<%#
  Shared service + city search — delegates to components/search_bar (single implementation).
  Options unchanged: variant, tenantUrlPrefix, formAction, formId, searchQuery, cityQuery,
  showCategoryFilter, selectedCategory, categories, ariaLabel, idPrefix.
-%>
<%- include('partials/components/search_bar') %>
