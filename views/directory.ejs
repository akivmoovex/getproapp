<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Directory | GetPro</title>
    <link rel="stylesheet" href="/styles.css?v=20260325a" />
  </head>
  <body class="<%= tenant.themeClass %>">
    <%- include('partials/site_header', { tenantUrlPrefix }) %>

    <div class="container" style="padding: 20px 0 8px;">
      <form class="pro-search pro-search--directory" action="<%= tenantUrlPrefix %>/directory" method="get" role="search" style="max-width:100%;margin-bottom:20px;">
        <div class="pro-ac" data-ac-list="service" data-watermark-text="Search: Electrician">
          <input
            id="dir-search-q"
            class="pro-ac-input"
            type="text"
            value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
            placeholder=""
            autocomplete="off"
            role="combobox"
            aria-autocomplete="list"
            aria-expanded="false"
            aria-controls="dir-search-q-dropdown"
          />
          <input type="hidden" class="pro-ac-hidden" name="q" id="dir-search-q-value" value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>" />
          <ul class="pro-ac-dropdown" id="dir-search-q-dropdown" role="listbox" hidden></ul>
          <p class="pro-ac-msg" hidden></p>
        </div>
        <div class="pro-ac" data-ac-list="city" data-watermark-text="Search: Lusaka">
          <input
            id="dir-search-city"
            class="pro-ac-input"
            type="text"
            value="<%= typeof cityQuery !== 'undefined' ? cityQuery : '' %>"
            placeholder=""
            autocomplete="off"
            role="combobox"
            aria-autocomplete="list"
            aria-expanded="false"
            aria-controls="dir-search-city-dropdown"
          />
          <input type="hidden" class="pro-ac-hidden" name="city" id="dir-search-city-value" value="<%= typeof cityQuery !== 'undefined' ? cityQuery : '' %>" />
          <ul class="pro-ac-dropdown" id="dir-search-city-dropdown" role="listbox" hidden></ul>
          <p class="pro-ac-msg" hidden></p>
        </div>
        <button type="submit" class="pro-search-dir-btn">
          <span>Search Professionals</span>
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="11" cy="11" r="7" />
            <path d="m21 21-4.3-4.3" />
          </svg>
        </button>
      </form>
      <div style="margin:0 0 18px;">
        <div class="section-title" style="margin-top:0;">
          <% if (selectedCategory) { %>
            Category: <%= selectedCategory %>
          <% } else if ((typeof searchQuery !== 'undefined' && searchQuery) || (typeof cityQuery !== 'undefined' && cityQuery)) { %>
            Search
            <% if (typeof searchQuery !== 'undefined' && searchQuery) { %>: “<%= searchQuery %>”<% } %>
            <% if (typeof cityQuery !== 'undefined' && cityQuery) { %><% if (typeof searchQuery !== 'undefined' && searchQuery) { %> · <% } else { %>: <% } %>City: <%= cityQuery %><% } %>
          <% } else { %>
            Browse categories
          <% } %>
        </div>
      </div>

      <div class="hero-grid" style="grid-template-columns: 0.9fr 1.1fr;">
        <div class="card">
          <div style="font-weight:900;margin-bottom:12px;">Categories</div>
          <div class="list">
            <a href="<%= tenantUrlPrefix %>/directory" class="btn" style="justify-content:flex-start;width:100%;">All</a>
            <% categories.forEach(function(cat) { %>
              <a
                href="<%= tenantUrlPrefix %>/directory?category=<%= cat.slug %>"
                class="btn"
                style="justify-content:flex-start;width:100%;<%= selectedCategory === cat.slug ? 'border-color:var(--wf-primary);background:var(--primary-soft-bg);' : '' %>"
              >
                <%= cat.name %>
              </a>
            <% }) %>
          </div>
        </div>

        <div>
          <div class="list">
            <% if (companies.length === 0) { %>
              <div class="card">No companies found.</div>
            <% } else { %>
              <% companies.forEach(function(c) { %>
                <a class="card" href="<%= buildCompanyUrl({ baseDomain: baseDomain, subdomain: c.subdomain }) %>" target="_blank" rel="noreferrer" style="display:block;">
                  <div class="row-between" style="gap:8px;">
                    <div style="font-weight:900;font-size:16px;"><%= c.name %></div>
                    <div class="muted" style="font-size:12px;">@<%= c.subdomain %></div>
                  </div>
                  <% if (c.headline) { %>
                    <div class="muted" style="margin-top:6px;line-height:1.5;"><%= c.headline %></div>
                  <% } %>
                </a>
              <% }) %>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <%- include('partials/site_footer', { tenantUrlPrefix }) %>
    <script src="/scripts.js?v=20260325a"></script>
    <script src="/autocomplete.js?v=20260325a"></script>
  </body>
</html>
