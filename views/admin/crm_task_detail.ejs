<!doctype html>
<html lang="en">
  <head>
    <%- include('../partials/brand_resolve') %>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Task #<%= task.id %> | CRM | <%= _bn %> Admin</title>
    <link rel="stylesheet" href="<%= asset('styles') %>" />
  </head>
  <body class="admin-app admin-crm-page<%- bodyEmbedClass %>">
    <%- include('../partials/admin_nav', { navTitle: 'CRM task', activeNav: 'crm' }) %>
    <main class="admin-main">
      <div class="container admin-main__pad">
        <div class="admin-toolbar admin-toolbar--compact">
          <%- include('../partials/components/button', { variant: 'default', href: '/admin/crm', label: '← CRM board' }) %>
        </div>
        <%- include('../partials/crm_task_inner') %>
      </div>
    </main>
  </body>
</html>
