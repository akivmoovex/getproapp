<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= category ? 'Edit' : 'New' %> Category | GetPro Admin</title>
    <link rel="stylesheet" href="/styles.css?v=20260320k" />
  </head>
  <body>
    <div class="container" style="padding: 22px 0;">
      <%- include('../partials/admin_nav', { navTitle: category ? 'Edit category' : 'New category', activeNav: 'categories' }) %>
      <div class="nav-links" style="margin-bottom:10px;">
        <a class="btn" href="/admin/categories">← All categories</a>
      </div>

      <div class="section-title" style="margin-top:14px;"></div>
      <div class="card" style="max-width: 720px;">
        <form
          method="post"
          action="<%= category ? '/admin/categories/' + category.id : '/admin/categories' %>"
          class="form"
        >
          <div>
            <label for="name">Category Name</label>
            <input id="name" name="name" value="<%= category ? category.name : '' %>" required />
          </div>
          <div>
            <label for="slug">Category Slug</label>
            <input id="slug" name="slug" value="<%= category ? category.slug : '' %>" required />
            <div class="muted" style="margin-top:6px;font-size:12px;line-height:1.5;">
              Example: `accounting`, `lawyers`, `electricians`.
            </div>
          </div>

          <button class="btn btn-primary" type="submit">Save</button>
        </form>
      </div>
    </div>
  </body>
</html>

