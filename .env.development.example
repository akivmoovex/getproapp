# Local / dev-staging — copy to `.env` for machine-local runs (NODE_ENV not production merges this file).
# For a public Hostinger site, set the same keys in the panel; production does not load `.env`.

NODE_ENV=production
BASE_DOMAIN=pronline.org
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
SESSION_SECRET=REPLACE_WITH_LONG_RANDOM_STRING
ADMIN_PASSWORD=REPLACE_ME
PUBLIC_SCHEME=https
PORT=3000
HOST=0.0.0.0
GETPRO_PG_SSL=no-verify
TRUST_PROXY=1

# Branding (pronline.org — Pro-online lockup; omit GETPRO_HTML_DATA_BRAND or set proonline)
GETPRO_PRODUCT_NAME=Pro-online
GETPRO_PRODUCT_NAME_GETPRO=GetPro
GETPRO_HTML_DATA_BRAND=proonline
