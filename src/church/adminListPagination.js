"use strict";

/**
 * Church admin list pagination — re-exports shared platform listQuery.
 * Prefer requiring `src/platform/http/listQuery` for new code.
 */

module.exports = require("../platform/http/listQuery");
