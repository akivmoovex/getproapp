# GetPro Church — PostgreSQL repositories

This folder will contain PostgreSQL repository files for church-specific tables. All future church tables should use the `church_` prefix, for example `church_organizations`, `church_branches`, `church_members`, and `church_events`.
