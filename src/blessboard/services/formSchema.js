"use strict";

/**
 * BlessBoard form schema — shared Moovex allowlist (platform).
 * Kept as a stable import path for existing BlessBoard callers.
 */

module.exports = require("../../platform/forms/formSchema");
