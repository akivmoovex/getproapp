# BlessBoard — static assets

CSS and static assets for the BlessBoard church module templates.

This folder will contain static church-specific assets such as CSS, JavaScript, default images, ministry placeholders, and church branding assets. Production images used by live templates should go here, not in `design-reference`.
