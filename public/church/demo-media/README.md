# BlessBoard public /sermons demo media

Placeholder assets until Phase 2 upload/storage.

- `sermon-demo.mp3` — ~60s neutral generated tone bed (safe to bundle; not a real sermon).
- `sermon-notes-demo.pdf` — one-page sample sermon notes.

Replace via church content / future `video_url`, `audio_url`, `pdf_url` fields.
Video embed URL is configured in `src/routes/church/publicPages.js` as `SERMON_DEMO_MEDIA.demoVideoEmbedUrl`.
